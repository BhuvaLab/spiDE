% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/AllClasses.R
\docType{class}
\name{SpiDEResults}
\alias{SpiDEResults}
\alias{SpiDEResults-class}
\alias{$,SpiDEResults-method}
\title{An S4 class to store multi-bandwidth spiDE results}
\usage{
\S4method{$}{SpiDEResults}(x, name)
}
\arguments{
\item{x}{an object of class SpiDEResults.}

\item{name}{a character, the slot to retrieve.}
}
\value{
Return value varies depending on method.
}
\description{
Container for one or more \link{SpiDEFit} objects (one per niche
bandwidth) plus the cross-bandwidth combined inference and the tidy
results table produced by \code{\link[=testSpiDE]{testSpiDE()}}.
}
\section{Slots}{

\describe{
\item{\code{fits}}{a list of \link{SpiDEFit} objects, one per bandwidth.}

\item{\code{sigma}}{a numeric, the bandwidth grid.}

\item{\code{condition}}{a character, the condition (colData column) tested;
\code{NA_character_} in "niche" mode.}

\item{\code{mode}}{a character, the design mode: "condition" or "niche". In "niche"
mode \code{condition} is \code{NA_character_} and the \code{results.celltype} /
\code{results.patient} tables are empty.}

\item{\code{index}}{a character, the index cell types considered.}

\item{\code{niche}}{a character, the niche cell types considered.}

\item{\code{covariates}}{a character, the nuisance covariates included.}

\item{\code{coldata}}{a DataFrame of sample-level metadata.}

\item{\code{gene.weights}}{a matrix of per-gene Cauchy-combination weights
(genes x bandwidth).}

\item{\code{p.cauchy.pos}}{a matrix, Cauchy-combined up-regulation p-values.}

\item{\code{p.cauchy.neg}}{a matrix, Cauchy-combined down-regulation p-values.}

\item{\code{results}}{a data.frame, the tidy results table (empty until
\code{\link[=testSpiDE]{testSpiDE()}} is run), keyed by (gene, ct_index, ct_niche, bandwidth).}

\item{\code{results.celltype}}{a data.frame of cell-type-specific response calls
keyed by (gene, ct_index), from the \code{CellType:condition} block. Empty
unless the design carries that block. Retrieved with
\code{results(object, type = "celltype")}.}

\item{\code{results.patient}}{a data.frame of patient-level response calls, one
abundance-weighted contrast per gene. Empty unless the design carries the
\code{CellType:condition} block. Retrieved with
\code{results(object, type = "patient")}.}

\item{\code{fdr}}{a numeric, the FDR threshold used.}

\item{\code{call}}{the matched call that produced the object.}
}}

