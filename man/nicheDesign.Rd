% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/design.R
\name{nicheDesign}
\alias{nicheDesign}
\title{Build a spiDE design matrix}
\usage{
nicheDesign(
  spe,
  condition,
  sigma,
  index = NULL,
  niche = NULL,
  covariates = character(),
  cell_type = "cell_type",
  name = "Niche",
  sample_id = "sample_id",
  random = c("none", "intercept", "slope"),
  ...
)
}
\arguments{
\item{spe}{a SpatialExperiment with a niche reducedDim for \code{sigma}.}

\item{condition}{a character, the colData column of the tested condition.}

\item{sigma}{a numeric, the bandwidth (a single value).}

\item{index, niche}{character vectors restricting index / niche cell types
(NULL = all).}

\item{covariates}{a character vector of nuisance colData columns.}

\item{cell_type}{a character, the colData column of cell type labels.}

\item{name}{a character, the niche reducedDim prefix.}

\item{sample_id}{a character, the colData column identifying samples
(patients); used only when \code{random != "none"}.}

\item{random}{one of "none" (fixed-effects design, the default), "intercept"
(add a per-sample random intercept) or "slope" (also add per-sample random
slopes on the niche covariates). The random-effect columns are penalised at
fit time to implement a mixed model via ridge (see the vignette).}

\item{...}{ignored.}
}
\value{
a list with \code{W} (the design matrix), \code{covtype} (a factor of column
types), and \code{coefmap} (a data.frame mapping columns to index/niche cells).
}
\description{
Constructs the neighbourhood-interaction design matrix for a single niche
bandwidth and tags each covariate by type ("CellType", "Niche", "Response",
"ResponseNiche", or "Other"). The scientifically important covariates are the
three-way \code{CellType:condition:niche} interactions ("ResponseNiche"), which
capture how expression within an index cell type changes with the condition
as a function of a niche cell type's local density. Symmetric self
interactions (an index cell type against its own density) are dropped. This
is an escape hatch for custom fits; most users should call \code{\link[=fitSpiDE]{fitSpiDE()}}.
}
\examples{
data(toySpiDE)
spe <- toySpiDE
spe <- buildNiches(spe, sigma = 20)
des <- nicheDesign(spe, condition = "condition", sigma = 20)
table(des$covtype)

}
