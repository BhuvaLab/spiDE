% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/AllGenerics.R, R/buildNiches.R
\name{buildNiches}
\alias{buildNiches}
\alias{buildNiches,ANY-method}
\title{Build niche covariate matrices}
\usage{
buildNiches(spe, ...)

\S4method{buildNiches}{ANY}(
  spe,
  sigma = c(10, 30, 50, 70),
  cell_type = "cell_type",
  sample_id = "sample_id",
  edge = TRUE,
  diggle = TRUE,
  name = "Niche",
  BPPARAM = BiocParallel::SerialParam(),
  ...
)
}
\arguments{
\item{spe}{a SpatialExperiment with spatial coordinates, cell type labels,
and sample identifiers.}

\item{...}{ignored.}

\item{sigma}{a numeric vector of kernel bandwidths (in the units of
\code{spatialCoords}), default \code{c(10, 30, 50, 70)}.}

\item{cell_type}{a character, the colData column holding cell type labels.}

\item{sample_id}{a character, the colData column identifying samples.}

\item{edge, diggle}{logicals passed to \code{spatstat.explore::densityfun}
for edge correction (default TRUE).}

\item{name}{a character, the prefix for the stored reducedDims (default
"Niche").}

\item{BPPARAM}{a BiocParallelParam for parallelising over samples.}
}
\value{
the input \code{spe} with one \code{reducedDim} per bandwidth added.
}
\description{
Computes, for each biological sample and each bandwidth \code{sigma}, a
Gaussian kernel density estimate of every cell type evaluated at each cell's
location. The resulting cells x cellTypes matrices are stored as
\code{reducedDim(spe, "Niche<sigma>")}, one per bandwidth, with a consistent
set of cell-type columns (cell types absent from a sample are set to 0).
}
\examples{
data(toySpiDE)
spe <- toySpiDE
spe <- buildNiches(spe, sigma = c(10, 20))
SingleCellExperiment::reducedDimNames(spe)

}
