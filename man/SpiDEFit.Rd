% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/AllClasses.R
\docType{class}
\name{SpiDEFit}
\alias{SpiDEFit}
\alias{SpiDEFit-class}
\alias{$,SpiDEFit-method}
\title{An S4 class to store a single-bandwidth spiDE model fit}
\usage{
\S4method{$}{SpiDEFit}(x, name)
}
\arguments{
\item{x}{an object of class SpiDEFit.}

\item{name}{a character, the slot to retrieve.}
}
\value{
Return value varies depending on method.
}
\description{
Stores the negative binomial GLM fit and Wald/Brown inference
for a single niche bandwidth. Several \code{SpiDEFit} objects (one per
bandwidth) are combined in a \link{SpiDEResults} container.
}
\section{Slots}{

\describe{
\item{\code{sigma}}{a numeric, the niche bandwidth (kernel standard deviation) used.}

\item{\code{ngenes}}{a numeric, the number of genes.}

\item{\code{ncells}}{a numeric, the number of cells/spots.}

\item{\code{W}}{a matrix, the design matrix (cells x covariates).}

\item{\code{two.sided}}{logical, TRUE when the within-gene combination used
two-sided p-values (Cauchy/ACAT); FALSE for Brown's method.}

\item{\code{se_patient}}{numeric, per-gene standard error of the abundance-weighted
patient-level response contrast; length 0 when the design has no
CellType:condition block.}

\item{\code{covtype}}{a factor, the covariate type of each column of \code{W}, one of
"CellType", "Niche", "Response", "ResponseNiche", "ResponseCellType",
"Other", or "Random"
(patient random-effect columns for the mixed-effects fit).}

\item{\code{coefmap}}{a DataFrame mapping each covariate to its index cell type,
niche cell type, and type.}

\item{\code{alpha}}{a matrix, the per-gene coefficients (genes x covariates).}

\item{\code{gmean}}{a numeric, the per-gene intercept (zero for the generic fit).}

\item{\code{psi}}{a numeric, the per-gene negative binomial dispersion.}

\item{\code{loglik}}{a numeric, the per-gene log-likelihood (used for Cauchy weights).}

\item{\code{re_group}}{a character (or NULL), the random-effect group of each column
of \code{W} (\code{NA} for fixed columns); NULL for a fixed-effects fit.}

\item{\code{tau2}}{a numeric (or NULL), the fitted random-effect variance components
(one per random-effect group); NULL for a fixed-effects fit.}

\item{\code{penalty}}{a numeric (or NULL), the per-column ridge penalty (\code{lambda.a})
used at fit time (0 on fixed columns, \code{1/tau2} on random columns).}

\item{\code{df}}{a numeric (or NULL), the Wald reference degrees of freedom. NULL for
a fixed-effects fit (normal reference); a scalar between-patient \code{S - 2}
under \code{df.method = "between"}; or a named per-tested-coefficient vector
(aligned to the columns of \code{t_stat}/\code{se}) under \code{df.method = "satterthwaite"}
— the Response main effect stays ~ \code{S - 2} while niche interactions, informed
by within-sample variation, get a larger df.}

\item{\code{t_stat}}{a matrix, per-gene Wald t-statistics (genes x covariates).}

\item{\code{se}}{a matrix, per-gene coefficient standard errors (genes x covariates).}

\item{\code{p.combined.pos}}{a matrix, combined p-values for up-regulation
(genes x (gene-level plus per-index-cell-type)). The within-gene combiner
is Brown's method or the Cauchy combination test (see \code{combine}).}

\item{\code{p.combined.neg}}{a matrix, combined p-values for down-regulation.}

\item{\code{sampling}}{a factor, cells used for GLM/dispersion estimation (from
\code{\link[SpaNorm:fitNB]{SpaNorm::fitNB()}}).}
}}

