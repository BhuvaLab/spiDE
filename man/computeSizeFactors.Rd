% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/AllGenerics.R, R/sizeFactors.R
\name{computeSizeFactors}
\alias{computeSizeFactors}
\alias{computeSizeFactors,ANY-method}
\title{Compute per-sample library-size offsets}
\usage{
computeSizeFactors(spe, ...)

\S4method{computeSizeFactors}{ANY}(
  spe,
  cell_type = "cell_type",
  sample_id = "sample_id",
  count = "nCount",
  area = "Area",
  name = "LS",
  ...
)
}
\arguments{
\item{spe}{a SpatialExperiment.}

\item{...}{ignored.}

\item{cell_type}{a character, the colData column of cell type labels.}

\item{sample_id}{a character, the colData column of sample identifiers.}

\item{count}{a character, the colData column of per-cell total counts
(e.g. total UMI or negative-probe counts).}

\item{area}{a character, the colData column of per-cell area.}

\item{name}{a character, the colData column to write the LS offset to
(default "LS").}
}
\value{
the input \code{spe} with a per-cell \code{name} column in colData.
}
\description{
Derives a per-sample library-size offset (LS) by computing, for each
(sample, cell type), a log size factor from total counts over total area,
centring these per cell type, and taking the median across cell types for
each sample. The offset is written to a per-cell \code{colData} column so it
can enter the model design as a nuisance covariate.
}
\examples{
data(toySpiDE)
spe <- toySpiDE
spe <- computeSizeFactors(spe, count = "nCount", area = "Area")
head(spe$LS)

}
